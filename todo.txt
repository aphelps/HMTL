HMTL TODO LIST
--------------

** Add a config file that allows the serial interfaces to be named

** Allow ProgramManager to handle programs that are not attached to a single
   output (e.g. the sequence program which controls multiple outputs internally).
   Currently handle_msg() rejects output indices > num_outputs, so programs
   intended to manage their own set of outputs (using stored manager->outputs
   references) have no valid output slot to register against.

